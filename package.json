{
  "name": "nodejs-express-router-middleware",
  "version": "1.0.0",
  "description": "Working with Express router.  app can have minimal funtionalities of its own, there can be other files that define most of the functionalities. Here folder 'routes' consists people.js which defines major functionalities. A middleware mounted in people.js to show that any middleware can be used in express routes.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Saif",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
